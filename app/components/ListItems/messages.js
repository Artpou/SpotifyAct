/*
 * ListItems Messages
 *
 * This contains all the text for the ListItems component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ListItems';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ListItems component!',
  },
});
