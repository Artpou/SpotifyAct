/*
 * ListItemsRow Messages
 *
 * This contains all the text for the ListItemsRow component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ListItemsRow';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ListItemsRow component!',
  },
});
